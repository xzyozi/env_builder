[project]
name = "env-builder"
version = "0.1.0"
description = "手元PCから踏み台越しにSSH接続し、参照元(src)を参考に構築先(dst)の環境を整えてbuildを通すための作業基盤"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "paramiko>=3.4",
]

[tool.uv]
package = false
